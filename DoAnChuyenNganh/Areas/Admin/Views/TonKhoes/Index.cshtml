
@model PagedList.IPagedList<DoAnChuyenNganh.Models.EF.TonKho>
@using PagedList.Mvc;

@{
    ViewBag.Title = "Danh sách tồn kho";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}



@section naviheader
{
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/home" class="nav-link">Trang chủ</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/tonkhoes" class="nav-link">Tồn kho</a>
        </li>
    </ul>
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Quản lý tồn kho</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/admin/home/">Trang chủ</a></li>
                    <li class="breadcrumb-item active">Tồn kho</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">

    <!-- Default box -->
    <div class="card">

        <div class="card-header d-flex justify-content-between align-items-center">
            <h3 class="card-title">Danh sách tồn kho</h3>
        </div>
        <div class="card-header d-flex justify-content-between align-items-center">
            <!-- Form tìm kiếm bên trái -->
            <form method="get" action="@Url.Action("Index")" class="form-inline">
                <input type="text" name="tim" value="@ViewBag.CurrentFilter"
                       class="form-control mr-2" style="width:350px;" placeholder="Nhập tên kho hoặc mã định danh biến thể..." />
                <button type="submit" class="btn btn-primary">Tìm kiếm</button>
            </form>

            <!-- Nút bên phải -->
            <div class="ml-auto">
                <a href="/admin/tonkhoes/create" class="btn btn-primary">Thêm mới</a>
                <a href="#" class="btn btn-danger" id="BtnDeleteAll">Xóa</a>
            </div>
        </div>

        <div class="card-body">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <!--Chọn tất cả bản ghi-->
                        <th><input type="checkbox" id="SelectAll" /></th>
                        <th>STT</th>
                        <th>@Html.DisplayNameFor(model => model.First().BienTheSanPham.SanPham.TenSanPham)</th>
                        <th>
                            @Html.DisplayNameFor(model => model.First().BienTheSanPham.SKU)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.First().Kho.TenKho)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.First().SoLuong)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.First().NgayCapNhat)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                @{
                    // Lấy vị trí bắt đầu của STT cho trang hiện tại
                    int stt = (Model.PageNumber - 1) * Model.PageSize + 1;
                }
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td><input type="checkbox" class="cbkItem" value="@item.TonKhoId" /></td>
                                <td>@stt</td>
                                <td>@item.BienTheSanPham.SanPham.TenSanPham</td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.BienTheSanPham.SKU)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Kho.TenKho)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.SoLuong)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.NgayCapNhat)
                                </td>
                                <td>
                                    <a href="@Url.Action("Edit", new { id = item.TonKhoId})" class="btn btn-primary">Sửa</a>
                                    <a href="@Url.Action("Details", new { id = item.TonKhoId })" class="btn btn-dark">Chi tiết</a>
                                    <a href="@Url.Action("Delete", new { id = item.TonKhoId })" class="btn btn-danger">Xóa</a>
                                </td>
                            </tr>
                            stt++;
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="7" style="text-align:center">Không có bản ghi nào</td>
                        </tr>

                    }
                </tbody>
            </table>
            <br />
            <div class="row">
                <div class="col-12 text-center" style="margin-bottom:8px; font-weight:bold;">
                    Trang @(Model.PageNumber) / @(Model.PageCount)
                    @*<br />*@
                    @*@Html.PagedListPager(Model, page => Url.Action("Index", new { page }))*@
                    @Html.PagedListPager(Model, page => Url.Action("Index", new { page, tim = Request.QueryString["tim"] }))

                </div>
            </div>
        </div>
    </div>
</section>



@section scripts{
    <script>
        $(document).ready(function () {
            // Chọn tất cả
            $('#SelectAll').click(function () {
                $('.cbkItem').prop('checked', this.checked);
            });

            // Xóa nhiều
            $('body').on('click', '#BtnDeleteAll', function (e) {
                e.preventDefault();
                var ids = [];
                $('.card-body').find('tr td input.cbkItem:checked').each(function () {
                    ids.push($(this).val());
                });

                if (ids.length === 0) {
                    alert("Vui lòng chọn ít nhất một kho.");
                    return;
                }

                if (confirm("Bạn có chắc chắn muốn xóa các mục đã chọn?")) {
                    $.ajax({
                        url: '/Admin/TonKhoes/DeleteAll',
                        type: 'POST',
                        data: { ids: ids.join(",") },
                        success: function (rs) {
                            if (rs.success) {
                                alert(rs.message);
                                location.reload();
                            } else {
                                alert(rs.message);
                            }
                        }
                    });
                }
            });
        });
    </script>
}
