@model PagedList.IPagedList<DoAnChuyenNganh.Models.EF.SanPham>
@using PagedList.Mvc;


@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}


@section naviheader
{
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/home" class="nav-link">Trang chủ</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/danhmucs" class="nav-link">Danh mục</a>
        </li>
    </ul>
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Quản lý sản phẩm</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/admin/home/">Trang chủ</a></li>
                    <li class="breadcrumb-item active">Sản phẩm</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">

    <!-- Default box -->
    <div class="card">

        <div class="card-header d-flex justify-content-between align-items-center">
            <h3 class="card-title">Danh sách sản phẩm</h3>
        </div>
        <div class="card-header d-flex justify-content-between align-items-center">
            <!-- Form tìm kiếm bên trái -->
            <form method="get" action="@Url.Action("Index")" class="form-inline">
                <input type="text" name="tim" value="@ViewBag.CurrentFilter"
                       class="form-control mr-2" style="width:350px;" placeholder="Nhập tên sản phẩm hoặc slug..." />
                <button type="submit" class="btn btn-primary">Tìm kiếm</button>
            </form>

            <!-- Nút bên phải -->
            <div class="ml-auto">
                <a href="/admin/sanphams/create" class="btn btn-primary">Thêm mới</a>
                <a href="#" class="btn btn-danger" id="BtnDeleteAll">Xóa</a>
            </div>
        </div>

        <div class="card-body">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <!--Chọn tất cả bản ghi-->
                        <th><input type="checkbox" id="SelectAll" /></th>
                        <th>STT</th>
                        <th>Ảnh</th>
                        <th>
                            @Html.DisplayNameFor(model => model.First().DanhMuc.TenDanhMuc)
                        </th>
                        @*<th>
                                @Html.DisplayNameFor(model => model.First().MaSanPham)
                            </th>*@
                        <th>
                            @Html.DisplayNameFor(model => model.First().TenSanPham)
                        </th>
                        @*<th>
                                @Html.DisplayNameFor(model => model.First().Slug)
                            </th>*@
                        <th>
                            @Html.DisplayNameFor(model => model.First().MoTaNgan)
                        </th>
                        @*<th>
                                @Html.DisplayNameFor(model => model.First().MoTaChiTiet)
                            </th>*@
                        <th>
                            @Html.DisplayNameFor(model => model.First().GiaBan)
                        </th>
                        @*<th>
                                @Html.DisplayNameFor(model => model.First().GiaGoc)
                            </th>*@
                        <th>
                            @Html.DisplayNameFor(model => model.First().TrangThai)
                        </th>
                        @*<th>
                                @Html.DisplayNameFor(model => model.First().LuotXem)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.First().NgayTao)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.First().NgayCapNhat)
                            </th>*@
                        <th></th>
                    </tr>
                </thead>
                @{
                    // Lấy vị trí bắt đầu của STT cho trang hiện tại
                    int stt = (Model.PageNumber - 1) * Model.PageSize + 1;
                }
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            var anhDaiDien = item.AnhSanPhams?.OrderBy(x => x.ThuTu).FirstOrDefault();  // lấy ảnh có thứ tự nhỏ nhất (thường =1)
                            <tr>
                                <td><input type="checkbox" class="cbkItem" value="@item.SanPhamId" /></td>
                                <td>@stt</td>
                                <td>
                                    @if (anhDaiDien != null)
                                    {
                                        <img src="@anhDaiDien.Url" alt="Ảnh sản phẩm" style="width:80px; height:120px; object-fit:cover;" />
                                    }

                                    else
                                    {
                                        <span>Không có ảnh</span>
                                    }
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DanhMuc.TenDanhMuc)
                                </td>
                                @*<td>
                                        @Html.DisplayFor(modelItem => item.MaSanPham)
                                    </td>*@
                                <td>
                                    @Html.DisplayFor(modelItem => item.TenSanPham)
                                </td>
                                @*<td>
                                        @Html.DisplayFor(modelItem => item.Slug)
                                    </td>*@
                                <td>
                                    @Html.Raw(item.MoTaNgan)
                                    @*@Html.DisplayFor(modelItem => item.MoTaNgan)*@
                                </td>
                                @*<td>
                                        @Html.DisplayFor(modelItem => item.MoTaChiTiet)
                                    </td>*@
                                <td>
                                    @Html.DisplayFor(modelItem => item.GiaBan)
                                </td>
                                @*<td>
                                        @Html.DisplayFor(modelItem => item.GiaGoc)
                                    </td>*@
                                <td>
                                    @Html.DisplayFor(modelItem => item.TrangThai)
                                </td>
                                @*<td>
                                        @Html.DisplayFor(modelItem => item.LuotXem)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.NgayTao)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.NgayCapNhat)
                                    </td>*@
                                <td>
                                    <a href="@Url.Action("Edit", new { id = item.SanPhamId})" class="btn btn-primary">Sửa</a>
                                    <a href="@Url.Action("Details", new { id = item.SanPhamId })" class="btn btn-dark">Chi tiết</a>
                                    <a href="@Url.Action("Delete", new { id = item.SanPhamId })" class="btn btn-danger">Xóa</a>
                                    <a href="@Url.Action("Create", "AnhSanPhams", new { sanPhamId = item.SanPhamId })" class="btn btn-success">Thêm hình ảnh</a>
                                </td>
                            </tr>
                            stt++;
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="7" style="text-align:center">Không có bản ghi nào</td>
                        </tr>

                    }
                </tbody>
            </table>
            <br />
            <div class="row">
                <div class="col-12 text-center" style="margin-bottom:8px; font-weight:bold;">
                    Trang @(Model.PageNumber) / @(Model.PageCount)
                    @*<br />*@
                    @Html.PagedListPager(Model, page => Url.Action("Index", new { page, tim = Request.QueryString["tim"] }))

                    @*@Html.PagedListPager(Model, page => Url.Action("Index", new { page }))*@
                </div>
            </div>
        </div>
    </div>
</section>


@section scripts{
    <script>
        $(document).ready(function () {
            // Chọn tất cả
            $('#SelectAll').click(function () {
                $('.cbkItem').prop('checked', this.checked);
            });

            // Xóa nhiều
            $('body').on('click', '#BtnDeleteAll', function (e) {
                e.preventDefault();
                var ids = [];
                $('.card-body').find('tr td input.cbkItem:checked').each(function () {
                    ids.push($(this).val());
                });

                if (ids.length === 0) {
                    alert("Vui lòng chọn ít nhất một sản phẩm.");
                    return;
                }

                if (confirm("Bạn có chắc chắn muốn xóa các sản phẩm đã chọn?")) {
                    $.ajax({
                        url: '/Admin/SanPhams/DeleteAll',
                        type: 'POST',
                        data: { ids: ids.join(",") },
                        success: function (rs) {
                            if (rs.success) {
                                alert(rs.message);
                                location.reload();
                            } else {
                                alert(rs.message);
                            }
                        }
                    });
                }
            });
        });
    </script>
}

