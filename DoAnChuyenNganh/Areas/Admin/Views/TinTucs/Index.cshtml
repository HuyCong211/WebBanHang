
@model PagedList.IPagedList<DoAnChuyenNganh.Models.EF.TinTuc>
@using PagedList.Mvc;

@{
    ViewBag.Title = "Danh sách tin tức";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}


@section naviheader
{
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/home" class="nav-link">Trang chủ</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/tintucs" class="nav-link">Tin tức</a>
        </li>
    </ul>
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Quản lý tin tức</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/admin/home/">Trang chủ</a></li>
                    <li class="breadcrumb-item active">Tin tức</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">

    <!-- Default box -->
    <div class="card">

        <div class="card-header d-flex justify-content-between align-items-center">
            <h3 class="card-title">Danh sách tin tức</h3>
        </div>
        <div class="card-header d-flex justify-content-between align-items-center">
            <!-- Form tìm kiếm bên trái -->
            <form method="get" action="@Url.Action("Index")" class="form-inline">
                <input type="text" name="tim" value="@ViewBag.CurrentFilter"
                       class="form-control mr-2" style="width:350px;" placeholder="Nhập tiêu đề hoặc slug..." />
                <button type="submit" class="btn btn-primary">Tìm kiếm</button>
            </form>

            <!-- Nút bên phải -->
            <div class="ml-auto">
                <a href="/admin/tintucs/create" class="btn btn-primary">Thêm mới</a>
                <a href="#" class="btn btn-danger" id="BtnDeleteAll">Xóa</a>
            </div>
        </div>

        <div class="card-body">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <!--Chọn tất cả bản ghi-->
                        <th><input type="checkbox" id="SelectAll" /></th>
                        <th>STT</th>
                        <th>Ảnh</th>
                        <th>
                            @Html.DisplayNameFor(model => model.First().TieuDe)
                        </th>
                        @*<th>
                            @Html.DisplayNameFor(model => model.First().Slug)
                        </th>*@
                        <th>
                            @Html.DisplayNameFor(model => model.First().TomTat)
                        </th>
                        @*<th>
                            @Html.DisplayNameFor(model => model.First().NoiDung)
                        </th>*@
                        <th>
                            @Html.DisplayNameFor(model => model.First().NgayDang)
                        </th>
                        @*<th>
                            @Html.DisplayNameFor(model => model.First().TrangThai)
                        </th>*@
                        <th></th>
                    </tr>

                </thead>
                @{
                    // Lấy vị trí bắt đầu của STT cho trang hiện tại
                    int stt = (Model.PageNumber - 1) * Model.PageSize + 1;
                }
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            var anhDaiDien = item.HinhAnhTinTucs?.OrderBy(x => x.Url).FirstOrDefault();  // lấy ảnh có thứ tự nhỏ nhất (thường =1)
                            <tr>
                                <td><input type="checkbox" class="cbkItem" value="@item.TinTucId" /></td>
                                <td>@stt</td>
                                <td>
                                    @if (anhDaiDien != null)
                                    {
                                        <img src="@anhDaiDien.Url" alt="Ảnh sản phẩm" style="width:120px; height:90px; object-fit:cover;" />
                                    }

                                    else
                                    {
                                        <span>Không có ảnh</span>
                                    }
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TieuDe)
                                </td>
                                @*<td>
                                    @Html.DisplayFor(modelItem => item.Slug)
                                </td>*@
                                <td>
                                    @Html.Raw(item.TomTat)
                                  
                                </td>
                                @*<td>
                                    @Html.Raw(item.NoiDung)
                             
                                </td>*@
                                <td>
                                    @Html.DisplayFor(modelItem => item.NgayDang)
                                </td>
                                @*<td>
                                    @Html.DisplayFor(modelItem => item.TrangThai)
                                </td>*@
                                <td>
                                    <a href="@Url.Action("Edit", new { id = item.TinTucId})" class="btn btn-primary">Sửa</a>
                                    <a href="@Url.Action("Details", new { id = item.TinTucId })" class="btn btn-dark">Chi tiết</a>
                                    <a href="@Url.Action("Delete", new { id = item.TinTucId })" class="btn btn-danger">Xóa</a>
                                    <a href="@Url.Action("Create", "HinhAnhTinTucs", new { TinTucId = item.TinTucId })" class="btn btn-success">Thêm hình ảnh</a>
                                </td>
                            </tr>
                            stt++;
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="7" style="text-align:center">Không có bản ghi nào</td>
                        </tr>

                    }
                </tbody>
            </table>
            <br />
            <div class="row">
                <div class="col-12 text-center" style="margin-bottom:8px; font-weight:bold;">
                    Trang @(Model.PageNumber) / @(Model.PageCount)
                    @*<br />*@
                    @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))
                </div>
            </div>
        </div>
    </div>
</section>




@section scripts{
    <script>
        $(document).ready(function () {
            // Chọn tất cả
            $('#SelectAll').click(function () {
                $('.cbkItem').prop('checked', this.checked);
            });

            // Xóa nhiều
            $('body').on('click', '#BtnDeleteAll', function (e) {
                e.preventDefault();
                var ids = [];
                $('.card-body').find('tr td input.cbkItem:checked').each(function () {
                    ids.push($(this).val());
                });

                if (ids.length === 0) {
                    alert("Vui lòng chọn ít nhất một tin tức.");
                    return;
                }

                if (confirm("Bạn có chắc chắn muốn xóa các tin tức đã chọn?")) {
                    $.ajax({
                        url: '/Admin/TinTucs/DeleteAll',
                        type: 'POST',
                        data: { ids: ids.join(",") },
                        success: function (rs) {
                            if (rs.success) {
                                alert(rs.message);
                                location.reload();
                            } else {
                                alert(rs.message);
                            }
                        }
                    });
                }
            });
        });
    </script>
}
